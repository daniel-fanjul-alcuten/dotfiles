# ===================================================================
# == dfanjul
# ===================================================================

map xD console delete
map xS shell tsp -m ionice -c 2 -n 7 shred -uv %d/%s
map xS shell tsp -m ionice -c 3 shred -uv %d/%s
map xl shell ln %c %d
map xs shell ln -s %c %d
