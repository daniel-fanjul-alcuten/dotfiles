#!/bin/bash
set -e

source ~/etc/backup.cfg

flock -x ~/etc/backup.cfg obnam --repository ~/.obnam backup $obnam_excludes --one-file-system $HOME
obnam --repository ~/.obnam forget --keep 72h,7d,5w,12m
