#!/bin/bash
set -e

find ~ -type d -xdev -execdir git rev-parse --git-dir \; 2>/dev/null |\
  sort -u |\
  xargs -rn 1 -I folder bash -c 'cd "folder" >&/dev/null && tsp -m -L "folder" ~/bin/git-fsck-and-gc >/dev/null'
