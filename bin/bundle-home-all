#!/bin/bash
set -e

source ~/etc/backup.cfg

tsp tsp -S 2 >/dev/null
# tsp -m ~/bin/bundle-backup-home biabam >/dev/null
for name in $backup_mountpoints; do
  tsp -m ~/bin/bundle-home "$name" >/dev/null
done
~/bin/tsp-update-limit 2
