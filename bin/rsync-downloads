#!/bin/bash
set -e

mountpoint="/media/dfs3ql"
if ! sudo mountpoint -q "$mountpoint"; then
  continue
fi

mkdir -p "$mountpoint"/downloads
~/bin/rsync-s3ql -v --stats --progress --partial ~/Downloads/ "$mountpoint"/downloads/
