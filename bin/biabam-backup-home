#!/bin/bash
set -e
set -x

mkdir -p ~/tmp
file=~/tmp/$(hostname)-home.bundle.git

cd ~
git bundle create "$file" --all
git bundle list-heads "$file" | biabam "$file" -s "$(hostname) home backup" daniel.fanjul.alcuten@gmail.com
rm -f "$file"
